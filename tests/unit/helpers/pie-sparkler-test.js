import { pieSparkler } from 'dummy/helpers/pie-sparkler';
import { module, test } from 'qunit';

module('Unit | Helper | pie sparkler');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pieSparkler([42]);
  assert.ok(result);
});
