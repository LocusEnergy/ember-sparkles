import { tickFilter } from 'dummy/helpers/tick-filter';
import { module, test } from 'qunit';

module('Unit | Helper | tick filter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tickFilter([42]);
  assert.ok(result);
});
