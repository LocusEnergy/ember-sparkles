import { debug } from 'dummy/helpers/debug';
import { module, test } from 'qunit';

module('Unit | Helper | debug');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = debug([42]);
  assert.ok(result);
});
