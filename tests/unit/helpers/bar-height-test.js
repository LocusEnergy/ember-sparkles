import { barHeight } from 'dummy/helpers/bar-height';
import { module, test } from 'qunit';

module('Unit | Helper | bar height');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = barHeight([42]);
  assert.ok(result);
});
