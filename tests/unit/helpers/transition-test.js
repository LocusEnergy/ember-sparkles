import { transition } from 'dummy/helpers/transition';
import { module, test } from 'qunit';

module('Unit | Helper | transition');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = transition([42]);
  assert.ok(result);
});
