
import { eSTimeFormatFn } from 'dummy/helpers/e-s/time-format-fn';
import { module, test } from 'qunit';

module('Unit | Helper | e s/time format fn');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eSTimeFormatFn([42]);
  assert.ok(result);
});

