import { emberSparklesTranslateX } from 'dummy/helpers/e-s/translate-x';
import { module, test } from 'qunit';

module('Unit | Helper | ember sparkles/translate x');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberSparklesTranslateX([42]);
  assert.ok(result);
});
