import { eSApplyMethod } from 'dummy/helpers/e-s/apply-method';
import { module, test } from 'qunit';

module('Unit | Helper | e s/apply method');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eSApplyMethod([42]);
  assert.ok(result);
});
