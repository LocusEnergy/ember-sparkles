
import { eSFormatFn } from 'dummy/helpers/e-s/format-fn';
import { module, test } from 'qunit';

module('Unit | Helper | e s/format fn');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eSFormatFn([42]);
  assert.ok(result);
});

