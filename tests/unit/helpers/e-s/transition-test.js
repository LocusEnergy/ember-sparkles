import { emberSparklesTransition } from 'dummy/helpers/e-s/transition';
import { module, test } from 'qunit';

module('Unit | Helper | ember sparkles/transition');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberSparklesTransition([42], { duration: 20 });
  assert.ok(result);
});
