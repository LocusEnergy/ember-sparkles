import { logReturn } from 'dummy/helpers/log-return';
import { module, test } from 'qunit';

module('Unit | Helper | log return');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = logReturn([42]);
  assert.ok(result);
});
