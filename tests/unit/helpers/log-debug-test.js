import { logDebug } from 'dummy/helpers/log-debug';
import { module, test } from 'qunit';

module('Unit | Helper | log debug');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = logDebug([42]);
  assert.ok(result);
});
