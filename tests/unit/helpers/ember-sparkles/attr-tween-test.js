import { emberSparklesAttrTween } from 'dummy/helpers/ember-sparkles/attr-tween';
import { module, test } from 'qunit';

module('Unit | Helper | ember sparkles/attr tween');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberSparklesAttrTween([42]);
  assert.ok(result);
});
