import { emberSparklesArcTween } from 'dummy/helpers/ember-sparkles/arc-tween';
import { module, test } from 'qunit';

module('Unit | Helper | ember sparkles/arc tween');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberSparklesArcTween([42]);
  assert.ok(result);
});
