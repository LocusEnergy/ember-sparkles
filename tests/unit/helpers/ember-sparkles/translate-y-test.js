import { emberSparklesTranslateY } from 'dummy/helpers/ember-sparkles/translate-y';
import { module, test } from 'qunit';

module('Unit | Helper | ember sparkles/translate y');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberSparklesTranslateY([42]);
  assert.ok(result);
});
