import { emberSparklesAxis } from 'dummy/helpers/ember-sparkles/axis';
import { module, test } from 'qunit';

module('Unit | Helper | ember sparkles/axis');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberSparklesAxis([42], { position: 'bottom' });
  assert.ok(result);
});
