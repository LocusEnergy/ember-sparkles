import { emberSparklesD3Select } from 'dummy/helpers/ember-sparkles/d3-select';
import { module, test } from 'qunit';

module('Unit | Helper | ember sparkles/d3 select');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberSparklesD3Select([42]);
  assert.ok(result);
});
