import { emberSparklesArc } from 'dummy/helpers/ember-sparkles/arc';
import { module, test } from 'qunit';

module('Unit | Helper | ember sparkles/arc');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberSparklesArc([42]);
  assert.ok(result);
});
