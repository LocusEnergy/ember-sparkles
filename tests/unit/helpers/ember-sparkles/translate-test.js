import { emberSparklesTranslate } from 'dummy/helpers/ember-sparkles/translate';
import { module, test } from 'qunit';

module('Unit | Helper | ember sparkles/translate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberSparklesTranslate([42]);
  assert.ok(result);
});
