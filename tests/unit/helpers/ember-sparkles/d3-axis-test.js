import { emberSparklesD3Axis } from 'dummy/helpers/ember-sparkles/d3-axis';
import { module, test } from 'qunit';

module('Unit | Helper | ember sparkles/d3 axis');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberSparklesD3Axis([42]);
  assert.ok(result);
});
