import { emberSparklesArcTransition } from 'dummy/helpers/ember-sparkles/arc-transition';
import { module, test } from 'qunit';

module('Unit | Helper | ember sparkles/arc transition');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberSparklesArcTransition([42]);
  assert.ok(result);
});
