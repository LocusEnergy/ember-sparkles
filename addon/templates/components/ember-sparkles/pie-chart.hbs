{{#if d3el}}

  {{!-- Write a helper for ember-sparkles/arc --}}
  {{#with (hash
    arc=(ember-sparkles/arc radius)
    labelArc=(ember-sparkles/arc radius true)
    pie=(ember-sparkles/pie valueFn=(pipe (d3-get 'value')))
  ) as |constructors|}}
  {{!-- {{#with (hash
    enterArc=(pipe
      (d3-append 'g')
      (d3-attr '')
    )
    updateArc=(pipe
    )
    exitArc=(pipe
    )
  ) as |renderOptions|}} --}}

    {{ember-d3-helpers/dev-null (compute
      (d3-join 'path' (compute constructors.pie data)
        enter=(pipe
          (d3-append 'path')
          (d3-attr 'transform' (i 'translate(${w},${h})' h=centerHeight w=centerWidth))
          (if with-transition (d3-transition transition) (d3-noop))
          (d3-attr 'd' constructors.arc)
          (d3-style 'fill' colorScale)
        )
        update=(pipe
          (d3-style 'fill' 'black')
          (if with-transition (d3-transition transition) (d3-noop))
          (d3-attr 'd' constructors.arc)
          (d3-style 'fill' colorScale)
        )
        exit=(pipe
          (d3-remove)
        )
    ) d3el)}}



    {{!-- {{ember-d3-helpers/dev-null (compute
      (d3-join 'text' (compute constructors.pie data)
      (d3-call (pipe
        (d3-append 'text')
        (d3-attr 'transform' (ember-sparkles/arc-translate constructors.labelArc.centroid))
        (d3-text (pipe (d3-get 'data.key')))
      ))
        enter=(pipe
          (d3-call (pipe
            (d3-append 'path')
            (d3-attr 'transform' (i 'translate(${w},${h})' h=centerHeight w=centerWidth))
            (d3-attr 'd' constructors.arc)
            (d3-style 'fill' colorScale)
          ))
          (d3-call (pipe
            (d3-append 'text')
            (d3-attr 'transform' (ember-sparkles/arc-translate constructors.labelArc.centroid))
            (d3-text (pipe (d3-get 'data.key')))
          ))
        )
        update=(pipe
          (d3-attr 'd' constructors.arc)
        )
        exit=(pipe
          (d3-remove)
        )
      ) d3el)}} --}}


  {{/with}}

{{/if}}
